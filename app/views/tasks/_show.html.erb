<%- model_class = Task -%>
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
  <!--h1><%=t '.title', :default => model_class.model_name.human %></h1-->
  <h1><%= @task.name%></h1>
</div>

<div class="modal-body row-fluid">

  <div class="span1">

    <dt><strong><%= model_class.human_attribute_name(:description) %>:</strong></dt>
    <dd><%= @task.description %></dd>
    <dt><strong><%= model_class.human_attribute_name(:priority) %>:</strong></dt>
    <dd><%= @task.priority %></dd>
    <dt><strong><%= model_class.human_attribute_name(:task_type) %>:</strong></dt>
    <dd><%= @task.task_type %></dd>

    <dt>Owner:</dt>
    <% @task.users.each do |user| %>
      <dd><%= user.email%> </dd>
    <% end %>
  </div>


  <div class="span8 offset2">
    <ul id="task_tab" class="nav nav-tabs">
        <li id="Comments" class="tab-pane active"><%= link_to "Comments", "#comments", 
        :data => {:remote => true, :toggle => "tab"},'data-type' => :html%></li>
        <li id="Bugs" class="tab-pane"><%= link_to "Bugs", "#bugs", :data => {:remote => true, :toggle => "tab"} %></li>
        <li id="Commits" class="tab-pane"><%= link_to "Commits", "#commits", :data => {:remote => true, :toggle => "tab"} %></li>
        <li id="Documentos" class="tab-pane"><%= link_to "Documents", "#documents", :data => {:remote => true, :toggle => "tab"} %></li>
    </ul>
  </div>

  <div class="tab-content span8 offset2">
    <div class="tab-pane active" id="comments">Coments ...</div>
    <div class="tab-pane" id="bugs">Bugs ...</div>
    <div class="tab-pane" id="commits">Commits ...</div>
    <div class="tab-pane" id="documents">Documents ...</div>
  </div>

</div>

<div class="modal-footer">
  <%= link_to "Close", "", :data => {:dismiss => "modal"}, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_project_status_task_path(@task.status.project, @task.status, @task), :remote => true, :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              project_status_task_path(@task.status.project, @task.status, @task),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>