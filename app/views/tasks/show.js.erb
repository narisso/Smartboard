$('#modal').modal('hide');
$('#modal-large').html("<%= escape_javascript(render 'show') %>");
$('#modal-large').modal('show');

var dds = $('#subtasks dd');
var as = dds.children();
$(dds).find('a').hide();

$(dds).hover(function(){
	$(this).find('a').show();
},
function(){
	$(this).find('a').hide();;
}
);