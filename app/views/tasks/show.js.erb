$('#modal-large').html("<%= escape_javascript(render 'show') %>");

var dds = $('#subtasks dd');
var as = dds.children();
$(dds).find('a').hide();

$(dds).hover(function(){
	$(this).find('a').show();
},
function(){
	$(this).find('a').hide();;
}
);